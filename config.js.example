// Spatialtour configuration — copy this to config.js and fill in your values
// config.js is gitignored — never commit real secrets

window.__SPATIALTOUR_CONFIG = {
  // OpenRouter API key for AI queries
  openrouterKey: 'sk-or-v1-your-key-here',
  // Default AI model
  model: 'google/gemini-2.0-flash-001',
  // Demo splat file URL
  demoSplat: '/samples/demo-room.splat',
};